const https = require('https');
const BREVO_API_KEY = process.env.BREVO_API_KEY;
async function sendBrevoEmail({to,subject,html}){return new Promise((resolve,reject)=>{const body=JSON.stringify({sender:{name:'WinTipsBet',email:'wintipsbetke@gmail.com'},to:[{email:to}],subject,htmlContent:html});const req=require('https').request({hostname:'api.brevo.com',path:'/v3/smtp/email',method:'POST',headers:{'Content-Type':'application/json','api-key':BREVO_API_KEY,'Content-Length':Buffer.byteLength(body)}},res=>{let d='';res.on('data',c=>d+=c);res.on('end',()=>res.statusCode<300?resolve(d):reject(new Error(d)))});req.on('error',reject);req.write(body);req.end()});}
async function sendWelcome({name,email}){await sendBrevoEmail({to:email,subject:'Welcome to WinTipsBet!',html:'<h2>Welcome '+name+'!</h2><p>Your account is ready. <a href=https://www.wintipsbet.com>View picks</a></p>'});}
async function sendPaymentConfirm({name,email,plan,amount,expiry,receipt}){await sendBrevoEmail({to:email,subject:'WinTipsBet - Payment Confirmed',html:'<h2>Payment Confirmed!</h2><p>Plan: '+plan+' | Amount: KSh '+amount+'</p>'});}
async function sendExpiryReminder({name,email,plan,expiry}){await sendBrevoEmail({to:email,subject:'WinTipsBet - Plan expires tonight',html:'<h2>Your plan expires tonight</h2><p>Renew at wintipsbet.com</p>'});}
module.exports={sendWelcome,sendPaymentConfirm,sendExpiryReminder};